"use client";
import { FormEvent, useState } from "react";
export function RegisterForm({pageId,webinarUrl,paymentUrl}:{pageId:string;webinarUrl:string;paymentUrl:string}){const [registered,setRegistered]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...data,pageId})});const d=await r.json() as {error?:string};setBusy(false);if(!r.ok){setError(d.error ?? "Registration failed");return}setRegistered(true)}
 if(registered)return <div className="watch-card"><span className="lp-eyebrow">YOU’RE REGISTERED</span><h2>Your webinar starts now</h2>{webinarUrl?<video controls autoPlay src={webinarUrl}><track kind="captions" srcLang="en" label="English captions"/></video>:<div className="video-empty">▶<small>Add a hosted video URL to play the webinar here.</small></div>}{paymentUrl&&<a className="lp-button" href={paymentUrl}>Choose your subscription →</a>}</div>;
 return <form className="register-card" onSubmit={submit}><span className="lp-eyebrow">FREE ON-DEMAND WEBINAR</span><h2>Get instant access</h2><p>Tell us where to send your resources. The webinar will start immediately.</p><input name="name" placeholder="Full name" required/><input name="email" type="email" placeholder="Email address" required/><input name="phone" placeholder="Phone number"/><input name="social" placeholder="Instagram, Facebook or X handle"/>{error&&<small className="form-error">{error}</small>}<button className="lp-button" disabled={busy}>{busy?"Registering...":"Register & watch now →"}</button><small>Your information is securely added to our CRM.</small></form>}
