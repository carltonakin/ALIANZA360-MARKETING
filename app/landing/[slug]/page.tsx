import { resolveSocialListenerConfig } from "../../api/social/_config";
import { RegisterForm } from "./RegisterForm";
type Landing={id:string;slug:string;headline:string;teaser:string;webinarUrl:string;paymentUrl:string;status:string};
async function loadPage(slug:string):Promise<Landing|null>{const config=await resolveSocialListenerConfig();if(!config)return null;try{const response=await fetch(`${config.serviceUrl}/content`,{headers:{authorization:`Bearer ${config.serviceToken}`},cache:"no-store"});if(!response.ok)return null;const body=await response.json() as {pages?:Landing[]};return body.pages?.find(page=>page.slug===slug&&page.status!=="archived")||null}catch{return null}}
export default async function LandingPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const page=await loadPage(slug);if(!page)return <main className="landing-shell"><div className="landing-missing"><h1>Page not found</h1><p>This webinar page may be unpublished, the link may be incorrect, or the SQL Server backend is unavailable.</p></div></main>;
 return <main className="landing-shell"><header className="landing-nav"><div className="brand-mark"><span>360</span></div><strong>Alianza Growth</strong><small>FREE • ON DEMAND • PRACTICAL</small></header><section className="landing-hero"><div className="teaser-side"><span className="lp-eyebrow">BUILD A BETTER GROWTH ENGINE</span><h1>{page.headline}</h1><p>{page.teaser||"Discover a practical system to attract the right audience, convert interest into qualified leads and build recurring revenue."}</p><div className="teaser-video"><span>▶</span><div><strong>Watch the 60-second preview</strong><small>See what you will learn inside</small></div></div><div className="proof"><span>✓ Actionable framework</span><span>✓ Watch instantly</span><span>✓ Free access</span></div></div><RegisterForm pageId={page.id} webinarUrl={page.webinarUrl} paymentUrl={page.paymentUrl}/></section></main>}
